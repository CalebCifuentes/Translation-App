[phases.setup]
nixPkgs = ["python312", "nodejs_22", "ffmpeg", "zlib"]

[phases.install]
cmds = [
  "npm ci",
  "python3 -m ensurepip --upgrade",
  "python3 -m pip install faster-whisper transformers peft gtts"
]

[start]
cmd = "node server.js"
